- dump, create: sanity check directory
* ensure related resources exist, e.g. script and its containing room

- scumm string parsing
- verify logic of jump-if opcodes

- query functionality; which scripts/actors/... are referenced/used where?